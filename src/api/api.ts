// export const baseUrl = "http://localhost:8080";
// as there is cors issue in localhost so when in dev mode I will add the contents locally to the remote database.

// const baseUrl =
//   process.env.NODE_ENV === "development"
//     ? "http://localhost:8080"
//     : "https://portfolio-api-eosin.vercel.app";

const baseUrl = "https://portfolio-api-eosin.vercel.app";
export { baseUrl };
