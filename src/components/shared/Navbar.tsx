import React from "react";
import ThemeToggler from "./ThemeToggler";

const navLinks = [{}];
const Navbar = () => {
  return (
    <nav>
      <ThemeToggler />
    </nav>
  );
};

export default Navbar;
